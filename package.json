{
  "name": "harlekid",
  "version": "1.0.0",
  "source": "index.html",
  "scripts": {
    "start": "parcel --no-cache",
    "build": "rm -rf ./dist && parcel build"
  },
  "author": "Kevin Duss",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "three": "^0.134.0"
  },
  "browserslist": [
    "defaults"
  ]
}
